<h2>Book search for "test"</h2>
<div class='col-md-12'>
<% if @records.any? %>
   <table class='table table-hover table-inverse'>
    <thead>
      <tr>
        <% if @model == "book" %>
          <th></th>
          <th>Title</th>
          <th>Opinion</th>
        <% else %>
          <th></th>
          <th>Name</th>
        <% end %>
      </tr>
    </thead>
    <tbody>
      <% @records.each do |record| %>
        <tr>
          <% if @model == "book" %>
            <td><%= image_tag record.book_image.attached? ? record.book_image : 'no_image.jpg', size: "50x50" %></td>
            <td><%= record.title %></td>
            <td><%= record.body %></td>
          <% else %>
            <td><%= image_tag record.get_profile_image, size: "50x50" %></td>
            <td><%= record.name %></td>
          <% end %>
        </tr>
      <% end %>
    </tbody>
  </table>
<% else %>
  <p>該当する結果が見つかりませんでした。</p>
<% end %>